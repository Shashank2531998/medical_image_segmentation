dataset:
  name: aeropath
  root: ./data/AeroPath/
  seed: 42
  batch_size: 1
  train_fraction: 0.0
  val_fraction: 0.0

model:
  dir: ./models/voxtell_v1.1
  device: cuda
  deep_supervision: false

evaluation:
  output_dir: ./experiments/evaluations/aeropath